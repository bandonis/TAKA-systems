Plan: Dashboard summary API + UI enhancement

1. Inspect current files
   - Read `app/(tenant)/dashboard/page.tsx` to see existing data fetching & cards.
   - Read `lib/tenant/api.ts` to understand `fetchTenantApi`.
   - Check for existing `/api/dashboard/summary` route; review implementation if present.
   - Review `lib/db` & `withTenantRoute` to ensure proper usage.

2. Backend improvements
   - If `/api/dashboard/summary` exists, refactor queries to use Prisma aggregates to produce:
     - `totalParticipantsLast30d`
     - `revenueAllTime` (paid receipts)
     - `upcomingEventsCount`
     - `pendingPaymentsCount`
   - Ensure route uses `withTenantRoute`, `getPrisma`, proper error handling.

3. Frontend updates
   - Update dashboard page to fetch summary via `fetchTenantApi('/api/dashboard/summary')`.
   - Render cards showing metrics; handle loading/error states gracefully.
   - Keep existing sections (coming up next, quick actions) unchanged.

4. Validation & reporting
   - Run `npm run lint` and `npm run build`.
   - Summarize file changes and test results for user.

